const numbers = [1, -2, 3, 4];

// Filter: Returns the elements of an array that meet the condition specified in a callback function. Returns array.

const filtered = numbers.filter(n => n >= 0);
console.log(filtered);
