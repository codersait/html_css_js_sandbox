const numbers = [1, 2, 3, 4];

// End
const last = numbers.pop(); // Removes the last element from an array and returns it.

//Beginning
const first = numbers.shift(); // Removes the first element from an array and returns it.

// Middle
numbers.splice(2, 1); // Removes elements from an array and, if necessary, inserts new elements in their place, returning the deleted elements.
