const numbers = [1, 2, 3];

const joined = numbers.join(' '); //returns string

const msg = 'hello my name is said';
const parts = msg.split(' '); // returns string array
// this technique is useful when building a URL slug.
const combined = parts.join('-');
